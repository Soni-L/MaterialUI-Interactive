export const fullPage = `
const {
    colors,
    CssBaseline,
    ThemeProvider,
    Typography,
    Container,
    makeStyles,
    createMuiTheme,
    Box,
    SvgIcon,
    Link,
    Button,
  } = MaterialUI;

  function ExtendedButton() {
    return (
      <Button variant="contained" color="primary">
          Primary
      </Button>
    );
  }`;
