export const textField = `
const {
    colors,
    Container,
    makeStyles,
    TextField,
  } = MaterialUI;


  function NumericInput() {
    return (
      <TextField variant="outlined" />
    );
}`;
